<project default="build_all">

	<target name="create_zip">
		<zip destfile="../Translit.zip" basedir="${basedir}" excludes=".*,res**"/>
	</target>

	<target name="build_all" depends="create_zip">
	</target>
</project>